<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeje.cashier">

    <application
        android:screenOrientation="portrait"
        android:allowBackup="false"
        android:icon="@drawable/cashier_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cashier_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cashier">
        <activity
            android:name=".Orders_Activity"
            android:exported="true" />
        <activity
            android:name=".Confirmation_Activity"
            android:exported="true" />
        <activity
            android:name=".CartActivity"
            android:exported="true" />
        <activity
            android:name=".Transaksi_Activity"
            android:exported="true" />
        <activity
            android:name=".MenuActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>